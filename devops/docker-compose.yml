services:
  library-backend:
    build:
      context: ..
      dockerfile: devops/Dockerfile

    container_name: library-backend

    restart: unless-stopped

    env_file:
      - ../.env

    networks:
      - postgres

networks:
  postgres:
    external: true